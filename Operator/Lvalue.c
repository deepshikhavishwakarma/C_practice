#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main() {
    
      
    //10=20;  Left value required in fn main    : Lvalue error means left side value not changeable.

    // 10+20 = 30; //lvalue required as left operand of assignment

     int a,b,c;
     system("cls");// Clear the screen on Windows
    a=b=c=100;
    printf("a=%d, b=%d, c=%d",a,b,c);      // o/p a = 100, b =100, c=100
     getchar();  // Wait for a key press
}\
