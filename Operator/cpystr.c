#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main() {
    char str[20]; 
    strcpy(str, "Deepshikha");
    system("cls");  // Clear the screen on Windows
    printf("name = %s\n", str);
   // getchar();  // Wait for a key press
}